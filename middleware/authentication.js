export default async ({ store, route, redirect }) => {
  // if (!store.getters['user/auth'] && route.name !== 'login' && route.name !== 'index') {
  //   return redirect('/login')
  // }
  //
  // if (store.getters['user/auth'] && route.name === 'login') {
  //   return redirect('/')
  // }
}
